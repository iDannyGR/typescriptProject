(()=>{
  let productPrice = 100;
  productPrice = 12;
  console.log('productPrice', productPrice)

  let customerAge:number = 28;
  customerAge= customerAge + 1;
  console.log('customerAge', customerAge)

  let productInStock:number;
  console.log(productInStock)
    productInStock >10 ? console.log('is greater') : null

  let discount = parseInt('400');
  console.log('discount ' + discount)
    discount >= 200 ? console.log('apply discount') : console.log('Not Aplly')

    let hex = 0xfff; // para cologar hexadecimal en TS se coloca primero 0x
    console.log(hex)

    let bin =0b1010; //para binario se utiliza 0b
    console.log(bin);

    const myNumber: number = 10;


  }
)();
